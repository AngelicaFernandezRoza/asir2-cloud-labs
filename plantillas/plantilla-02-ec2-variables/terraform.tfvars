# Edita este fichero con tus valores.
# ES EL UNICO FICHERO QUE CAMBIAS PARA ADAPTAR EL PROYECTO.
#
# Terraform asigna automáticamente cada valor de aquí a la variable del
# mismo nombre declarada en variables.tf, sin que haya que tocar main.tf.

nombre_alumno  = "tu-nombre"       # Cambia esto por tu nombre real
tipo_instancia = "t2.micro"        # Tamaño de la instancia (puedes dejarlo así)
ami_id         = "ami-XXXXXXXXXX"  # Tu AMI ID del paso 1 (obligatorio, no funciona tal cual)
